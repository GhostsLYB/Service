2020-3-8
	完善数据库表，与客户端表结构相似，便于数据同步

2020-3-11
	为服务器添加查看动态的客户端连接信息功能
	重新整理服务器项目文件的文件结构（头文件最好不要有全局变量）
	改进makefile，修改编译步骤（之前的没有条理）
2020-3-12
	为所有没有时间字段的表添加时间字段，便于同步数据
	改进服务器查看动态信息功能
2020-3-13
	添加数据同步功能
	需同步的数据：各个表导出的txt文件, 存放位置 /var/lib/mysql_files/
	  user_info user_friendList userName_chatInfo recend_chatInfo ...
	  命名方式 userName_tableName.txt(哪个用户的哪个表导出的txt文件)
	逻辑：将客户端ip与上次登陆的IP对比
	  1.如果与上次登陆IP相同则同步上次退出以后的数据（如果没有则不需要同步）
	  2.如果与上次登陆IP不相同，则分两种情况：
	  （1）该登陆IP登陆过，则同步该IP上次登陆退出以后新数据
	  （2）该登陆IP未登陆过，则同步所有数据
	（数据导出部分完成，缺少文件发送）
2020-3-14
	
2020-4-17
	
